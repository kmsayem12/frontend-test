export const apiUrl = `${process.env.REACT_APP_BACKEND_URL}/`;

export const authUrl = `${apiUrl}login/`;

export const devicesUrl = `${apiUrl}devices/`;

export const notifyUrl = `${apiUrl}notify/`;
